# Copy to .env and fill in. Never commit .env.

# From @BotFather
BOT_TOKEN=

# Must match restaurants.id in the database (Admin → restaurant row)
RESTAURANT_ID=1

# Kitchen Telegram group (numeric, often negative e.g. -100...)
KITCHEN_GROUP_ID=

# If this machine cannot reach https://api.telegram.org (ETIMEDOUT / firewall):
# SKIP_TELEGRAM_KITCHEN=true
